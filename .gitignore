# Minimal public sync — only user skills, rules, and slash commands.
# Everything else under ~/.cursor stays local.

*

!.gitignore

!skills/
!skills/**

!rules/
!rules/**

!commands/
!commands/**

# Repository documentation (tracked for GitHub)
!README.md

